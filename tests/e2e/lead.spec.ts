import { test } from "@playwright/test"

test.skip(true, "Live lead capture requires a dedicated Supabase project. Not executed here.")
test("assessment creates a lead visible to the admin", async () => {})
