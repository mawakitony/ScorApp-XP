import { test } from "@playwright/test"

test.skip(true, "Platform admin E2E was not executed against Supabase.")
test("an owner cannot open platform and a support session can exit @platform", async () => {})
